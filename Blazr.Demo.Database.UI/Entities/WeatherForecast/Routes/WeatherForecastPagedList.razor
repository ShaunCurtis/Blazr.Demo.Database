@page "/weatherforecast/pagedlist"
@page "/weatherforecast/list"
@page "/weatherforecast/"
@page "/weatherforecasts"
@namespace Blazr.Demo.Database.UI
@attribute [Authorize(Policy = "IsVisitor")]

<WeatherForecastListHeader />

<WeatherForecastListForm RouteId=RouteId />

@code {
    private static Guid RouteId = Guid.NewGuid();
}

