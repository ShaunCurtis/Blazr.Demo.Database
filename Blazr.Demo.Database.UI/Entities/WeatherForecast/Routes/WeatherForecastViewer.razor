@page "/weatherforecast/view/{Id:guid}"
@attribute [Authorize(Policy = "IsUser")]

<WeatherForecastViewForm Id=this.Id></WeatherForecastViewForm>

@code {
    [Parameter] public Guid Id { get; set; }
}
